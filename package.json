{
  "//": "We purely split them into these different categories in order to do slightly more efficient installing in the Dockerfile",
  "devDependencies": {
    "elm-spa": "^6.0.4"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "optionalDependencies": {
    "elm-doc-preview": "^5.0.5",
    "elm-format": "^0.8.7",
    "elm-live": "^4.0.2",
    "elm-review": "^2.10.3",
    "elm-test": "^0.19.1-revision12",
    "elm-verify-examples": "^6.0.0"
  }
}
